import 'package:flutter/material.dart';

class MriUploadTab extends StatelessWidget {
  const MriUploadTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("MRI Upload Tab Content"),
    );
  }
}
