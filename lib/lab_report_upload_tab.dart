import 'package:flutter/material.dart';

class LabReportUploadTab extends StatelessWidget {
  const LabReportUploadTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Lab Report Upload Tab Content"),
    );
  }
}
