import 'package:flutter/material.dart';

class ClassificationAssistantTab extends StatelessWidget {
  const ClassificationAssistantTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Classification Assistant Tab Content"),
    );
  }
}
